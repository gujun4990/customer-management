# End-to-End Business Flow Test Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现端到端业务流程测试 - 从登录到持续的客户跟进

**Tech Stack:** Tauri + Rust + SQLx + PostgreSQL

---

### Task 1: 编写失败的测试

**Files:**
- Create: `customer-management/src-tauri/tests/e2e_flow.rs`

测试用例: `test_full_business_flow`
1. 用户登录 (sign_in)
2. 创建 lead
3. 分配 owner
4. 添加 follow-up
5. 转换为 customer
6. 继续跟进 customer

流程：
```rust
// 1. Login
let session = SessionState::default();
authenticate_record(&admin_creds, "admin123", &session)?;

// 2. Create lead
let lead = create_lead_for_session(&pool, &session, CreateLeadInput{...})?;

// 3. Owner already assigned (creator is owner)

// 4. Add follow-up
let follow_up = create_lead_followup_for_session(&pool, &session, &lead_id, ...)?;

// 5. Convert to customer
let customer = convert_lead_to_customer_for_session(&pool, &session, &lead_id, "active")?;

// 6. Continue follow-up on customer
let customer_follow_up = create_customer_followup_for_session(&pool, &session, &customer_id, ...)?;
```

---

### Task 2: 运行测试验证通过 (GREEN)

- [ ] **Step 1: 运行测试**

```bash
cd customer-management/src-tauri && cargo test --test e2e_flow
```

Expected: PASS (因为功能已实现)

---

### Task 3: 更新 tasks.md 状态

- [ ] **Step 1: 将 task 6.4 标记为完成**